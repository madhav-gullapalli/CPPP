class Box {
    List<int> values;

    Box(int count, int value) {
        values = List<int>(count, value);
    }

    int get(int index) {
        return values[index];
    }
}

Box box = Box(3, 7);
print(box.get(1));
