struct Node {
    int value;
    Node next;
}

List<int> listFirst = [1];
List<int> listAlias = listFirst;
listAlias[0] = 2;

Set<int> setFirst = {1};
Set<int> setAlias = setFirst;
setAlias.add(2);

Map<int, int> mapFirst = {1:2};
Map<int, int> mapAlias = mapFirst;
mapAlias[1] = 3;

Pair<int, int> pairFirst = (1,2);
Pair<int, int> pairAlias = pairFirst;
pairAlias[0] = 4;

Set<Pair<int, int>> ordered = {(2,0), (1,0)};
Node objectFirst = Node(5, NULL);
Node objectAlias = objectFirst;
objectAlias.value = 6;
print(listFirst[0], 2 in setFirst, mapFirst[1], pairFirst[0], ordered, objectFirst.value);
