bool descending(int a, int b) {
    return a > b;
}

struct Point {
    int x;
    int y;
}

List<int> values = [3, 1, 2];
values.sort(descending);
Set<int> ranked(greater);
ranked.add(2);
ranked.add(4);
ranked.add(1);
Map<int, int> scores(descending);
scores[2] = 20;
scores[4] = 40;
scores[1] = 10;
List<List<int>> rows = [[8, 3], [1, 7], [5, 2]];
rows.sort(compare(1));
List<Point> points = [Point(2, 8), Point(1, 3)];
points.sort(compare("y"));
print(values, ranked, scores, rows, points);
