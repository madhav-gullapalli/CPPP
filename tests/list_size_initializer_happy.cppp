class Node {
    int value;
}
List<int> numbers(5);
List<Map<int, int>> maps(2);
maps[0][1] = 7;
string text(5);
List<Node> nodes(2);
List<List<int>> rows(4);
rows[0].add(8);
List<List<int>> grid(2, 3);
grid[0][0] = 9;
List<string> words(1, 3);
print(len(numbers), numbers[4], len(maps), len(maps[1]), len(text));
print(nodes, len(rows[0]), len(rows[1]), grid[0][0], grid[1][0], len(words[0]));
