int t = input();
rep(t){
    int n = input();
    List<int> A = input(n);
    List<int> B = input(n);
    int less, more = 0,0;
    for(int i = 0;i<n;i++){
        if(A[i]<B[i]){less+= B[i]; less-=A[i];}
        else{more+=A[i]; more -=B[i];}
    }
    print(more+1);
}