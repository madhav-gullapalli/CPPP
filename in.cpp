#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    vector<long long> empt = vector<long long>{};
    cout << ([&]() { auto __cppp_list = empt; return *min_element(__cppp_list.begin(), __cppp_list.end()); }()); cout << '\n';
    return 0;
}
